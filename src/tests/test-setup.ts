// Test setup file - imported before all tests
import 'reflect-metadata';

// Disable colors in terminal output for consistent test assertions
process.env.FORCE_COLOR = '0';
process.env.NODE_ENV = 'test';
